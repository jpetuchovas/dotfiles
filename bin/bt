#!/usr/bin/env bash
# %/ removes the trailing forward slash if it exists.
directory="${1%/}"
# While loop is used because -d flag causes entr to exit if new files are added.
while true
do
  find "$directory" | entr -cds "bazel test //$directory/..."
done
