- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
      force: true
    ~/.condarc:
      force: true
    ~/.config/flake8:
      path: config/flake8
    ~/.direnvrc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.gnupg/gpg-agent.conf:
      path: gnupg/gpg-agent.conf
    ~/.inputrc:
    ~/.isort.cfg:
    ~/.mypy.ini:
    ~/.nanorc:
    ~/.psqlrc:
    ~/.pydocstylerc:
    ~/.shell:
