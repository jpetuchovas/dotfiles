- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
      force: true
    ~/.condarc:
      force: true
    ~/.direnvrc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.inputrc:
    ~/.nanorc:
    ~/.psqlrc:
    ~/.shell:
